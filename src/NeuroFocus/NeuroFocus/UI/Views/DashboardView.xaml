<UserControl x:Class="NeuroFocus.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeuroFocus.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" 
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             Background="{StaticResource ThemeClr}">
    <UserControl.Resources>
        
    </UserControl.Resources>
    <Grid Margin="20, 30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Greetings, FallbackValue='Good afternoon'}"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="#fff"
                           FontFamily="{StaticResource Outfit}"/>

                <TextBlock Text="{Binding presetDesc, FallbackValue='Your environment is optimized for calm focus'}"
                           FontSize="16"
                           FontWeight="Regular"
                           FontFamily="{StaticResource Outfit}"
                           Foreground="{StaticResource TextClr}"
                           Margin="0,5,0,0"/>
            </StackPanel>

            <Border Grid.Column="1" Padding="20,10" CornerRadius="20" VerticalAlignment="Top" Background="{StaticResource StatusBg}">
                <TextBlock Text="{Binding SystemStatus, FallbackValue='System Active'}" 
                           FontFamily="{StaticResource Outfit}"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource StatusText}"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
